MainScreen:

<MainScreen>:
    BoxLayout:
        size_hint: (.1, .1)
        TextInput:
            # id: my_text_input
            # font_size: "40dp"
            # color: 1, .5, 1
            size_hint: (4, 1)
            # multiline: True
            # size_hint_y:100
        Button:
            text: "Enter"
    
                
